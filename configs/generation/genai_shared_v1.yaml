version: genai_shared_v1
status: shared_plan_ready_backend_smoke_tests_pending

source:
  copy_paste_metadata: data/synthetic/cp_v1_seed42/metadata.json
  copy_paste_generation_config: data/synthetic/cp_v1_seed42/generation_config.json
  rationale: reuse_pre_results_frozen_conditions_for_paired_generator_comparison

allocation:
  classes: 16
  canonical_images: 2048
  nested_prefixes: [512, 1024, 1536, 2048]
  images_per_class: 128
  pilot_images_per_class: 2
  pilot_images_per_backend: 32

conditioning:
  inference_width: 1024
  inference_height: 1024
  initialization: reconstructed_undegraded_copy_paste_composite
  initialization_uses_rgba_object_reference: true
  control_reference: canny_edges_of_initialization
  inpaint_mask: expanded_intended_object_bbox
  source_background_is_preserved_outside_mask: true
  bbox_margin_ratio: 0.25
  minimum_bbox_margin_source_pixels: 4
  placement_source: frozen_cp_v1_metadata
  annotation_source: post_generation_sam3_mask
  intended_bbox_is_not_accepted_as_final_annotation: true

seeds:
  stable_diffusion_offset: 142000
  qwen_offset: 242000

degradation:
  reuse_copy_paste_v1_schedule: true
  application_stage: after_generation_and_annotation_acceptance
  paired_by_image_id_across_backends: true

release_gate:
  plan_ready: true
  sdxl_smoke_passed: false
  qwen_smoke_passed: false
  annotation_smoke_passed: false
  pilot_review_passed: false
  production_approved: false
